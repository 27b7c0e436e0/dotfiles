# Aliases

alias ls='ls --color=always'
alias grep='grep --color=always -n'
alias ll='ls -ltr'
alias la='ls -lAtr'
alias l='ls -CF'
alias t='tailf'
alias tree='tree -C'
alias vrc='vim ~/.bashrc'
alias brc='source ~/.bashrc'

# EOF
